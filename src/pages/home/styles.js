const gridStyle = () => ({
  root: {
    marginTop: 10,
    marginBottom: 10,
    padding: 10,
    borderRadius: 10,
    height: "100vh",
    backgroundColor: "var(--color-background2)",
    boxShadow: "var(--box-shadow1)",
  }
});

export { gridStyle };