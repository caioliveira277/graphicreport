const gridStyle = () => ({
  root: {
    marginTop: 10,
    marginBottom: 10,
    padding: 10,
    borderRadius: 10,
    height: "100vh",
    backgroundColor: "var(--color-theme3)",
    boxShadow: "var(--box-shadow-1)",
  }
});

export { gridStyle };